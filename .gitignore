# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/tfplan*
terraform/crash.log
terraform/*.tfvars

# Python
__pycache__/
*.pyc
.venv/
.venv-test/
venv/
multi-agent-ai-research-platform/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
evals/golden_results.json

# Env / secrets
.env
*.pem

# Personal (not project source — contains PII, doesn't belong in a public repo)
akash.tex
*.pdf

# OS
.DS_Store
